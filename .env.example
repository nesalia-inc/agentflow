# Database Configuration
AGENTFLOW_DB_URL=postgresql+asyncpg://user:password@localhost:5432/agentflow

# Optional: Schema name (defaults to 'public')
# AGENTFLOW_DB_SCHEMA=agentflow

# Optional: Connection pool settings
# AGENTFLOW_DB_POOL_SIZE=10
# AGENTFLOW_DB_MAX_OVERFLOW=20
